version: '3.8'

services:
  web:
    build: .
    container_name: django_chat
    ports:
      - "8054:8054"
    volumes:
      - .:/app
    environment:
      - DEBUG=1
    command: sh -c "python manage.py migrate && daphne -b 0.0.0.0 -p 8054 chat_project.asgi:application"
